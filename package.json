{
	"devDependencies": {
		"lerna": "^2.11.0"
	},
	"scripts": {
		"publish": "lerna publish",
		"bootstrap": "lerna bootstrap",
		"test": "lerna run test",
		"build": "lerna run build",
		"watch": "lerna run --parallel watch"
	}
}
